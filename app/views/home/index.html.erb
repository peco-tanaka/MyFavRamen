<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyFavらぁめん - 自分だけのラーメンランキング</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <style>
        /* --- Custom Styles (ファイル保存時にUTF-8形式であることを確認してください) --- */
        :root {
            /* Bootstrapのカラー変数をオレンジ系でオーバーライド */
            --bs-primary: #F57C00;
            --bs-primary-rgb: 245, 124, 0;
            --bs-secondary: #FFB74D;
            --bs-secondary-rgb: 255, 183, 77;
            --bs-dark-orange: #BF360C;
            --bs-light-orange: #FFF8E1;
            --bs-text-dark-orange: #BF360C;
            --bs-text-orange: #E65100;

            /* Bootstrap Body Colors */
            --bs-body-bg: var(--bs-light-orange);
            --bs-body-color: #4A4A4A;
            --bs-body-font-family: 'Noto Sans JP', 'Helvetica Neue', Arial, sans-serif;
            --bs-link-color: var(--bs-primary);
            --bs-link-hover-color: #E65100;

            --bs-border-radius: 0.5rem;
            --bs-border-radius-pill: 50rem;
        }

        body {
            font-family: var(--bs-body-font-family);
            background-color: var(--bs-body-bg);
            color: var(--bs-body-color);
        }

        /* Header Customizations */
        .navbar {
            background: linear-gradient(to right, #FFA726, #FB8C00);
            border-bottom: 3px solid #E65100;
            padding-top: 0.8rem;
            padding-bottom: 0.8rem;
        }
        .navbar .navbar-brand {
            font-size: 1.7rem;
            font-weight: 700;
            transition: transform 0.3s ease;
        }
         .navbar .navbar-brand:hover {
             transform: scale(1.05);
         }
        .navbar .nav-link {
            font-weight: 500;
            padding: 0.5rem 1rem;
            border-radius: var(--bs-border-radius-pill);
            transition: background-color 0.3s ease, color 0.3s ease, box-shadow 0.3s ease;
            margin-left: 0.5rem;
        }
        .navbar .nav-link:hover,
        .navbar .nav-link.active {
            background-color: rgba(255, 255, 255, 0.2);
        }
        .navbar .nav-link.btn-signup {
             background-color: #E65100;
             color: #fff !important;
             font-weight: 600;
        }
        .navbar .nav-link.btn-signup:hover {
             background-color: var(--bs-dark-orange);
             box-shadow: 0 4px 8px rgba(0,0,0,0.2);
        }

        /* Hero Section Customizations */
        .hero {
            background: url('https://images.unsplash.com/photo-1557872943-16a5ac26437e?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1770&q=80') center center/cover no-repeat;
            position: relative;
            color: #fff;
            overflow: hidden;
        }
        .hero::before { /* Overlay */
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, rgba(251, 140, 0, 0.75), rgba(191, 54, 12, 0.85));
        }
        .hero .container {
            position: relative;
            z-index: 1;
        }
        .hero h1 {
            color: #fff;
            font-size: 3.2rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
            animation: fadeInDown 1s ease-out;
        }
        .hero h2 {
            color: #FFEBCC;
            font-size: 1.5rem;
            font-weight: 400;
            animation: fadeInUp 1s ease-out 0.3s;
            animation-fill-mode: backwards;
        }
        .hero p.description {
            font-size: 1.1rem;
            color: #FFF3E0;
             max-width: 700px;
             margin-left: auto;
             margin-right: auto;
             animation: fadeInUp 1s ease-out 0.6s;
            animation-fill-mode: backwards;
        }
         .hero .btn-container {
             animation: fadeInUp 1s ease-out 0.9s;
             animation-fill-mode: backwards;
         }

        /* Button Customizations */
        .btn-primary {
             background-color: var(--bs-primary);
             border-color: var(--bs-primary);
             color: #fff;
             transition: all 0.3s ease;
             box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
         .btn-primary:hover {
             background-color: #E65100;
             border-color: #E65100;
             transform: translateY(-3px);
             box-shadow: 0 6px 15px rgba(245, 124, 0, 0.4);
         }
        .btn-secondary {
             background-color: var(--bs-secondary);
             border-color: var(--bs-secondary);
             color: #4E342E;
             transition: all 0.3s ease;
              box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .btn-secondary:hover {
             background-color: #FFE0B2;
             border-color: #FFE0B2;
             color: #BF360C;
             transform: translateY(-3px);
             box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
        }

        /* Features Section Customizations */
        .features {
            background-color: #fff;
        }
        .features .section-title {
             color: var(--bs-text-dark-orange);
             margin-bottom: 3rem;
             font-size: 2.3rem;
              border-left: 5px solid var(--bs-secondary);
              padding-left: 15px;
              display: inline-block;
        }
        .feature-card {
            border: none;
            border-radius: var(--bs-border-radius);
            box-shadow: 0 8px 25px rgba(255, 152, 0, 0.12);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            height: 100%;
            border-top: 4px solid var(--bs-secondary);
        }
        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 35px rgba(255, 112, 67, 0.2);
        }
        .feature-icon {
            font-size: 3rem;
            color: var(--bs-primary);
            margin-bottom: 1.5rem;
        }
        .feature-card .card-title {
             color: var(--bs-text-orange);
             font-weight: 600;
        }

        /* Auth Links Section */
        .auth-links {
             background-color: #FFF3E0;
             border-top: 1px solid #FFE0B2;
             border-bottom: 1px solid #FFE0B2;
        }
         .auth-links .prompt {
              color: var(--bs-text-dark-orange);
              font-weight: 500;
              font-size: 1.3rem;
          }
           .auth-links .forgot-password-link {
               font-size: 0.95rem;
               color: #6c757d;
           }
           .auth-links .forgot-password-link:hover {
                color: var(--bs-link-hover-color);
            }

        /* Footer Customizations */
        .footer {
            background-color: var(--bs-dark-orange);
            color: #FFEBCC;
        }
         .footer p {
             margin-bottom: 0;
         }

        /* Animations */
        @keyframes fadeInDown { from { opacity: 0; transform: translateY(-20px); } to { opacity: 1; transform: translateY(0); } }
        @keyframes fadeInUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

        /* Responsive Adjustments */
         @media (max-width: 767.98px) {
              .hero h1 { font-size: 2.6rem; }
              .hero h2 { font-size: 1.3rem; }
              .features .section-title { font-size: 1.9rem; }
              .feature-card { margin-bottom: 1.5rem; }
         }
          @media (max-width: 575.98px) {
               .hero h1 { font-size: 2.2rem; }
               .hero h2 { font-size: 1.1rem; }
               .hero p.description { font-size: 1rem; }
               .navbar .navbar-brand { font-size: 1.5rem; }
               .btn { padding: 0.6rem 1.2rem; font-size: 0.95rem; }
                .auth-links .btn { width: 75%; margin-bottom: 1rem; }
          }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark sticky-top">
        <div class="container">
            <a class="navbar-brand" href="/">🍜 MyFavらぁめん</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
                    <li class="nav-item"><a class="nav-link active" aria-current="page" href="/">ホーム</a></li>
                    <li class="nav-item"><a class="nav-link" href="/rankings">ランキングを見る</a></li>
                    <li class="nav-item"><a class="nav-link" href="<%= new_user_session_path %>">ログイン</a></li>
                    <li class="nav-item"><a class="nav-link btn btn-signup" href="<%= new_user_registration_path %>">新規登録</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <section class="hero py-5">
        <div class="container text-center py-lg-5">
            <h1 class="mb-3">MyFavらぁめん</h1>
            <h2 class="mb-4">感動の一杯を、あなただけのランキングに。</h2>
            <p class="description mb-5">
                MyFavらぁめんは、あなたのお気に入りのラーメン店を記録し、自分だけのランキングを作成・共有できる場所。<br>
                評価ではなく「好き」を大切にする、ポジティブなラーメンコミュニティです。
            </p>
            <div class="btn-container">
                <a href="<%= new_user_registration_path %>" class="btn btn-primary btn-lg px-4 me-sm-3">今すぐランキングを作る (無料)</a>
                <a href="/rankings" class="btn btn-secondary btn-lg px-4">他の人のランキングを見る</a>
            </div>
        </div>
    </section>

    <section class="features py-5">
        <div class="container">
             <div class="text-center text-lg-start mb-5">
                <h3 class="section-title">MyFavらぁめんの楽しみ方</h3>
             </div>
            <div class="row g-4">
                <div class="col-lg-4 col-md-6 d-flex align-items-stretch">
                    <div class="card feature-card text-center p-4">
                        <div class="card-body d-flex flex-column">
                            <i class="bi bi-trophy-fill feature-icon"></i>
                            <h4 class="card-title mt-3">あなただけのランキング</h4>
                            <p class="card-text mt-auto">食べたラーメン店を記録して、自分だけの「俺ランク」を作成。総合、ジャンル別など自由にカスタマイズ！お店への評価機能はありません。</p>
                        </div>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6 d-flex align-items-stretch">
                    <div class="card feature-card text-center p-4">
                         <div class="card-body d-flex flex-column">
                             <i class="bi bi-pencil-square feature-icon"></i>
                            <h4 class="card-title mt-3">食べた記録をシンプルに</h4>
                            <p class="card-text mt-auto">お店の名前、訪問日、写真、そしてトップ3には熱い想いを込めた一言コメントも。感動した一杯の記憶を、かんたんに、いつまでも残せます。</p>
                        </div>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6 d-flex align-items-stretch">
                    <div class="card feature-card text-center p-4">
                         <div class="card-body d-flex flex-column">
                             <i class="bi bi-people-fill feature-icon"></i>
                            <h4 class="card-title mt-3">ラーメン好きとゆるく繋がる</h4>
                            <p class="card-text mt-auto">他の人のランキングを見て、新しいお店を発見！フォロー機能で気になるユーザーの更新をチェック。批判のない平和な空間で交流を楽しめます。</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

     <section class="auth-links py-5">
        <div class="container text-center">
            <p class="prompt mb-4">さあ、あなただけのラーメン道をはじめよう！</p>
            <a href="<%= new_user_registration_path %>" class="btn btn-primary btn-lg px-5 me-sm-3 mb-3 mb-sm-0">新規登録はこちら</a>
            <a href="<%= new_user_session_path %>" class="btn btn-secondary btn-lg px-5">ログインはこちら</a>
            <div class="mt-4">
                 <a href="<%= new_user_password_path %>" class="forgot-password-link">パスワードをお忘れですか？</a>
            </div>
        </div>
    </section>

    <footer class="footer py-4 mt-auto">
        <div class="container text-center">
            <p>&copy; 2025 MyFavらぁめん. All Rights Reserved.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>