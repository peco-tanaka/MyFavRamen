<%# Offcanvas ナビゲーション (スマホ: ドロワー / PC: サイドバー) %>
<%# PC表示時の固定用に sidebar-lg クラスを追加 %>
<div class="offcanvas-lg offcanvas-end text-bg-light sidebar-lg " tabindex="-1" id="offcanvasNavbar" aria-labelledby="offcanvasNavbarLabel">
  <div class="offcanvas-header d-lg-none">
    <h5 class="offcanvas-title" id="offcanvasNavbarLabel">メニュー</h5>
    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" data-bs-target="#offcanvasNavbar" aria-label="Close"></button>
  </div>
  <div class="offcanvas-body d-flex flex-column p-2">
    <%# ナビゲーションメニュー %>
    <nav class="nav nav-pills flex-column">
      <%= link_to edit_user_path(current_user), class: "nav-link link-dark" do %>
        <i class="bi bi-pencil-square me-2"></i> プロフィール編集
      <% end %>
      <%= link_to destroy_user_session_path, class: "nav-link link-danger", data: { turbo_method: :delete, turbo_confirm: "ログアウトしますか？" } do %>
        <i class="bi bi-box-arrow-right me-2"></i> ログアウト
      <% end %>
    </nav>
    <div class="mt-auto d-none d-lg-block">
      <hr>
      <small class="text-muted">© Your App Name</small>
    </div>
  </div>
</div>
<%# スマホ用header %>
<nav class="navbar navbar-expand-lg sticky-top d-lg-none shadow-sm py-1" style="background-color: #fd7e14 !important; height: 49.2px;">
  <div class="container-fluid d-flex align-items-center">
    <!-- ジャンル選択ボタン -->
    <div class="genre-buttons-container mx-auto d-flex overflow-auto">
      <div class="d-flex gap-1" role="tablist">
        <% if @genres.present? %>
          <% @genres.each do |genre| %>
            <%= link_to edit_genre_ranking_rankings_path(genre_id: genre.id),
                class: "btn #{@current_genre&.id == genre.id ? 'btn-danger' : 'btn-outline-light'} px-2 py-0 rounded-pill btn-sm",
                style: "font-size: 0.9rem; white-space: nowrap; font-weight: bold;" do %>
              <%= genre.name %>
            <% end %>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</nav>